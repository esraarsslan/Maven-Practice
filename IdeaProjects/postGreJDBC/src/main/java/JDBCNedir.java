public class JDBCNedir {

    /**+
     * JDBC nedir?
     * Java JDBC (Java DataBase Connectivity) MySQL, Oracle, MS SQL Server gibi veritabanlarına bağlanmak veri çekme,
     * listeleme, ekleme, silme, güncelleme gibi işlemleri yapmak için kullanılan pakettir.
     *
     * JDBC API kullanımı için gerekli olan sınıflar java.sql paketinde yer alır.
     *
     * JDBC yapısı veritabanından bağımsız olduğundan SQL destekleyen tüm ilişkisel veritabanı ile birlikte çalışır.
     *
     * Örneğin; MySQL veritabanında yer alan öğrenci listesi Oracle veritabanına taşınıp oradan alınmak istendiğinde
     * sadece bağlantı cümlesinin değiştirilmesi yeterli olacaktır.
     */

    /*
    JDBC kullanımı
==JDBC API kullanımı veritabanı sürücünün yüklenmesi, veritabanı bağlantısı,
SQL sorgusunun gönderilmesi ve sonuçların alınması adımlarından oluşur.

==Veritabanı bağlantısı için öncelikle kullanılacak olan veritabanı sistemine ait bağlantı sürücüsünün projeye eklenmesi gerekir.

==Bu işlem komut yorumlayıcısına jar dosyasının dahil edilmesi,
IDE arayüzünde libraries bölümüne eklenmesi veya Maven gibi paket yöneticilerinin kullanımı ile yapılabilir.
     */
}
